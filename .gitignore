# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
.vercel

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local

# typescript
*.tsbuildinfo

# แคปหน้าจอดิบจากเกม — ไม่ commit (ดู docs/PLAN.md §6)
# แต่ไฟล์ตั้งค่าพิกัดครอปต้อง commit ไม่งั้นคนที่ clone ไปรันสคริปต์ไม่ได้
/assets-src/*
!/assets-src/codex-crop.json
