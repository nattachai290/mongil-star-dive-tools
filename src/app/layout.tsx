/**
 * โครงรากของ App Router — ตัว <html> จริงอยู่ใน [locale]/layout.tsx
 * เพราะแอตทริบิวต์ lang ต้องเปลี่ยนตามภาษาของหน้านั้น
 */
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
